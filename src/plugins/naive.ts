import {
    create,
    NButton,
    NConfigProvider,
    NGlobalStyle,
    NCard,
    NTabs,
    NTabPane,
    NForm,
    NFormItemRow,
    NInput,
    NSwitch
} from 'naive-ui';

const naive = create({
    components: [
        NButton,
        NConfigProvider,
        NGlobalStyle,
        NCard,
        NTabs,
        NTabPane,
        NForm,
        NFormItemRow,
        NInput,
        NSwitch
    ]
})

export default naive